// Reusa o mesmo render do opengraph-image. Twitter/X aceita 1200x630
// (summary_large_image) idêntico ao OG, então não precisamos de variante.
export { runtime, alt, size, contentType, default } from './opengraph-image';
